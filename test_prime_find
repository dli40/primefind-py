from prime_find import *
import unittest


class TestCase(unittest.TestCase):
    def testPrimeFind(self):
        self.assertEqual(prime_find(10), [2, 3, 5, 7])

    def testLarger(self):
        self.assertEqual(prime_find(20), [2, 3, 5, 7, 11, 13, 17, 19])

    if __name__ == '__main__':
        unittest.main()
